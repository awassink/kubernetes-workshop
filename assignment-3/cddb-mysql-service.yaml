apiVersion: v1
kind: Service
metadata:
  labels:
    app: cddb-mysql
    visualize: "true"
  name: cddb-mysql
spec:
  ports:
  - port: 3306
    protocol: TCP
    targetPort: 3306
  selector:
    app: cddb-mysql
  sessionAffinity: None
  type: ClusterIP
